<%#encoding:utf-8 %>
<div class="hero-unit">
  <h1>Estados</h1>
</div>

<%= link_to "Crear nuevo mensaje", new_status_path , class: "btn btn-success" %>

<% @statuses.each do |status| %>
  <div class = "status" >
    <strong><%= status.user.full_name%></strong>
    <p><%= status.content %></p>
      <div class = "class">
        <%= link_to time_ago_in_words(status.created_at) + " después", status %> |
        <%= link_to "Mostrar", status %>
        <span class="admin" >
          <%= link_to "Editar", edit_status_path(status) %> |
          <%= link_to "Borrar", status, method: :delete, data: { confirm: "Estas seguro de borrar?"} %>
        </span>
      </div>
  </div>
<% end %>